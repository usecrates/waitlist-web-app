# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Set Secrets and Change Dir
        run: | # Secrets needed on the server
          echo "NEXT_PUBLIC_PRIVY_APP_ID=${{vars.NEXT_PUBLIC_PRIVY_APP_ID}}" >> .env.production
          echo "NEXT_PUBLIC_BACKEND_BASE_URL=${{vars.NEXT_PUBLIC_BACKEND_BASE_URL}}" >> .env.production
          echo "NEXT_PUBLIC_DINARI_API_KEY_ID=${{vars.NEXT_PUBLIC_DINARI_API_KEY_ID}}" >> .env.production
          echo "NEXT_PUBLIC_ASSETTOKEN=${{vars.NEXT_PUBLIC_ASSETTOKEN}}" >> .env.production
          echo "NEXT_PUBLIC_RPC_URL=${{vars.NEXT_PUBLIC_RPC_URL}}" >> .env.production
          echo "NEXT_PUBLIC_NODE_ENV=${{vars.NEXT_PUBLIC_NODE_ENV}}" >> .env.production
          echo "NEXT_PUBLIC_DINARI_API_SECRET_KEY=${{secrets.NEXT_PUBLIC_DINARI_API_SECRET_KEY}}" >> .env.production
          echo "NEXT_PUBLIC_PAYMENTTOKEN=${{secrets.NEXT_PUBLIC_PAYMENTTOKEN}}" >> .env.production
          pwd
          ls -lAh
          cat .env.production
      - uses: actions/checkout@v4
      # - run: npm ci
      - run: npm install -f
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_USECRATES }}
          channelId: live
          projectId: usecrates
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
